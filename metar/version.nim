## The version.nim file defines the version numbers of metar and its
## required components.

## Nimpy is not required for metar exe, just for the metar python lib.

const metarVersion* = "0.1.25"
const nimpyVersion* = "0.1.0-c8ec14a"
